require("Comment").setup {
    toggler = {
        line = "<Space>lc",
        block = "<Space>bc",
    },
    -- LHS of operator-pending mappings in NORMAL and VISUAL mode
    opleader = {
        line = "<Space>lc",
        block = "<Space>bc",
    },
}
